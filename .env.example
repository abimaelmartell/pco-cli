# Create a Planning Center personal access token or OAuth app credentials,
# then set either a bearer token or an app id/secret pair.
PCO_ACCESS_TOKEN=
PCO_APP_ID=
PCO_SECRET=
PCO_BASE_URL=https://api.planningcenteronline.com
PCO_USER_AGENT=pco-cli/0.1.0
